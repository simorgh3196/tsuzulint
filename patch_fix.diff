--- crates/tsuzulint_cache/src/manager.rs
+++ crates/tsuzulint_cache/src/manager.rs
@@ -749,5 +749,9 @@
         let file = File::create(&cache_file).unwrap();
         file.set_len(MAX_CACHE_SIZE).unwrap();

-        let result = manager.load();
+        let _result = manager.load();
     }
 }
