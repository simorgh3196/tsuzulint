{
  "name": "tsuzulint-rule-{{RULE_NAME}}",
  "version": "0.1.0",
  "description": "TsuzuLint rule: {{RULE_DESCRIPTION}}",
  "type": "module",
  "scripts": {
    "build": "asc assembly/index.ts --target release",
    "build:debug": "asc assembly/index.ts --target debug",
    "test": "node --test tests/"
  },
  "devDependencies": {
    "assemblyscript": "^0.27.0",
    "@aspect/as-pdk": "^0.3.0",
    "json-as": "^0.9.0"
  },
  "exports": {
    ".": {
      "import": "./build/release.wasm"
    }
  },
  "files": [
    "build/release.wasm"
  ]
}
